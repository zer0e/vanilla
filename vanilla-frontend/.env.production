# 生产 / 云端后端地址。
# 1) 前端与后端同源部署（如 nginx 将静态文件与 /vanilla 反代放同一站点）：留空，走 /vanilla 同源路径
# 2) 前后端分离部署：改成云端后端地址，例如：
# VITE_API_BASE=http://47.111.15.0:8080
#
# 注意：分离部署时后端需允许前端来源的跨域请求（CORS），否则浏览器会拦截。
VITE_API_BASE=
