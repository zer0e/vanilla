services:
  mysql:
    image: 'd-proxy.re0.top/library/mysql:8.0'
    environment:
      - 'MYSQL_ROOT_PASSWORD=root'
    ports:
      - '3306:3306'
    volumes:
      - mysql:/var/lib/mysql
  redis:
    image: 'redis:7.2'
    ports:
      - '6379:6379'

volumes:
  mysql:
